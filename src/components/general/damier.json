{
  "collectionName": "components_general_damiers",
  "info": {
    "displayName": "Damier",
    "description": ""
  },
  "options": {},
  "attributes": {
    "title": {
      "type": "string"
    },
    "image": {
      "type": "media",
      "multiple": false,
      "required": true,
      "allowedTypes": [
        "images"
      ]
    },
    "image_side": {
      "type": "enumeration",
      "enum": [
        "Gauche",
        "Droite"
      ],
      "default": "Gauche"
    },
    "title_placement": {
      "type": "enumeration",
      "enum": [
        "Au-dessus",
        "Dans le contenu"
      ],
      "default": "Au-dessus"
    },
    "content": {
      "type": "customField",
      "options": {
        "output": "HTML",
        "preset": "rich"
      },
      "customField": "plugin::ckeditor.CKEditor",
      "required": true
    }
  }
}
